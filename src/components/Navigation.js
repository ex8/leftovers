import React from 'react';
import { AppBar } from '@material-ui/core';

const Navigation = () => {
  return (
    <div>
      <AppBar
        position="fixed"
      >

      </AppBar>
    </div>
  );
};

export default Navigation;
