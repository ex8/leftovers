import {
  createMuiTheme
} from '@material-ui/core/styles';

export default createMuiTheme({
  palette: {
    primary: {
      main: '#DA3743',
    },
    secondary: {
      main: '#77C3CC',
    },
    // type: "dark",
  },
});
